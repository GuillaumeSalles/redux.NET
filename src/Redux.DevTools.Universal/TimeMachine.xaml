<UserControl
    x:Class="Redux.DevTools.Universal.TimeMachine"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Redux.DevTools.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Slider
            x:Name="SignalPositionsSlider"
            Minimum="0"
            Maximum="0"
            HorizontalAlignment="Stretch"
            Margin="20"
            ValueChanged="SignalPositionsSlider_ValueChanged"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Current Signal"/>
            
            <TextBlock x:Name="CurrentSignalTypeTextBlock"/>
            <TextBlock x:Name="CurrentSignalDescription"/>
        </StackPanel>
    </Grid>
</UserControl>
